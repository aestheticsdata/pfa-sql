import { defineMessages } from 'react-intl';

const scope = 'app.components.langMenu.LangMenu';

export default defineMessages({
  languageLabel: {
    id: `${scope}.languageLabel`,
    defaultMessage: 'Language',
  },
});

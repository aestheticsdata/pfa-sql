import { defineMessages } from 'react-intl';

const scope = 'app.components.Register';

export default defineMessages({
  buttonLabel: {
    id: `${scope}.buttonLabel`,
    defaultMessage: 'Sign up',
  },
});

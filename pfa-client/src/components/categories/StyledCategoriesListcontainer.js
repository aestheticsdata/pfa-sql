import styled from 'styled-components';
import cssSizes from "@src/css-sizes";

const StyledCategoriesListContainer = styled.div`
  .categories-list-container {
    position: relative;
    top: ${cssSizes.navbarHeight}px;
    left: 10px;
    padding-top: 10px;
  }
`;

export default StyledCategoriesListContainer;

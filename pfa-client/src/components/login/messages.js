import { defineMessages } from 'react-intl';

const scope = 'app.components.Login';

export default defineMessages({
  buttonLabel: {
    id: `${scope}.buttonLabel`,
    defaultMessage: 'Login',
  },
  forgotPassword: {
    id: `${scope}.forgotPassword`,
    defaultMessage: 'Forgot password ?',
  },
});

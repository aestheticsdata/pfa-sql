import { defineMessages } from 'react-intl';

const scope = 'app.components.notFoundComponents';

export default defineMessages({
  text: {
    id: `${scope}.text`,
    defaultMessage: 'This page does not exist',
  },
});
